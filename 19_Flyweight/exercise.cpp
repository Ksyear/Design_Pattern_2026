// [연습] 플라이웨이트 패턴 - 텍스트 에디터의 글자 객체
//
// 고전적인 사례(GoF 원본 책의 예제)다.
//   문서 한 페이지에 글자가 3000개. 글자마다 Character 객체를 만들면
//   폰트/크기/굵기 정보가 3000번 복사된다.
//
// TODO 1. CharacterStyle(폰트명, 크기, 굵기, 색)을 플라이웨이트로 만들 것 - 전부 const
// TODO 2. StyleFactory로 같은 스타일은 재사용할 것
// TODO 3. Glyph { char c; int x, y; const CharacterStyle* style; } 를 문맥 객체로
// TODO 4. 문단 하나(500자)를 만들고 실제 CharacterStyle 인스턴스 수를 세어 볼 것
// TODO 5. sizeof(Glyph)와 '스타일을 값으로 품은 버전'의 sizeof를 비교할 것
//
// 함정 확인
// TODO 6. CharacterStyle에서 const를 빼고 크기를 바꿔 볼 것
//         -> 같은 스타일을 쓰는 글자 전체가 한꺼번에 바뀐다
//         이것이 "특정 인스턴스만 다르게 만들 수 없다"는 단점의 실체
//
// 싱글턴과의 차이
//   - 싱글턴: 인스턴스가 정확히 1개, 보통 가변
//   - 플라이웨이트: 인스턴스가 '내부 상태 종류만큼' 여러 개, 반드시 불변
//
// 언제 쓰지 말아야 하는가
//   객체 수가 수만 개 미만이면 대개 측정부터 해 봐야 한다.
//   CPU를 더 쓰고(외부 상태 재계산) 코드가 복잡해지는 대가가 먼저 온다

#include <iostream>

int main() {
	std::cout << "TODO: 텍스트 에디터 글자 플라이웨이트를 만들어 보세요\n";
	return 0;
}
